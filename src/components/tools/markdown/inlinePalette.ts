/**
 * 行内样式（字号 / 文字色 / 背景色）的取值表。
 *
 * 选中浮条（SelectionToolbar）与工具栏「格式」菜单（ToolbarMenus）共用同一份，
 * 两处点出来的东西必须一模一样 —— 这里抽出来就是为了不给它们各自留一份能写歪的副本。
 */

/**
 * 字号档位。数值与 Cherry 的 Size 菜单一一对应（12/17/24/32），
 * 写出来的标记也一致，两个内核共用一份 Markdown 时必须对齐。
 */
export const FONT_SIZES = [
  { px: 12, label: '小' },
  { px: 17, label: '中' },
  { px: 24, label: '大' },
  { px: 32, label: '超大' },
] as const

/**
 * 文字颜色。Cherry 的调色盘是一整块 HSV 拾色器（含最近使用色），
 * 这里只取常用色做成一格小色板 —— 做不了同等保真度，够用即可。
 */
export const TEXT_COLORS = [
  '#e60000',
  '#ff8000',
  '#e6b800',
  '#2e9e4f',
  '#0099ff',
  '#6600cc',
  '#999999',
  '#1f2937',
] as const

/**
 * 文字背景色。**带透明度**，不是实色 —— 实色在深色主题下会把正文的字压得看不见，
 * 而底色是写进 Markdown 的内联样式，CSS 覆盖不掉，只能一开始就选一个明暗两种主题
 * 下都读得清的写法。半透明色叠在任意底色上都只是一层淡色，两种主题都不影响阅读，
 * 项目自己的高亮（`<mark>`）走的也是这条路子。
 */
export const BACKGROUND_COLORS = [
  'rgba(230, 0, 0, 0.32)',
  'rgba(255, 128, 0, 0.32)',
  'rgba(230, 184, 0, 0.38)',
  'rgba(46, 158, 79, 0.3)',
  'rgba(0, 153, 255, 0.3)',
  'rgba(102, 0, 204, 0.28)',
  'rgba(100, 116, 139, 0.28)',
  'rgba(236, 72, 153, 0.3)',
] as const

/**
 * 「插入」菜单里的表情。取常用的一小批，不做完整 emoji 检索 ——
 * 真要找冷门表情，系统自带的输入法比任何自绘面板都好用。
 */
export const EMOJI_LIST = [
  '😀', '😄', '😊', '🙂', '😉', '😍', '🤔', '😅',
  '😂', '😭', '😢', '😮', '😴', '🤯', '😱', '🥳',
  '👍', '👎', '👌', '🙏', '👏', '💪', '🤝', '✌️',
  '❤️', '🔥', '✨', '🎉', '💡', '⭐', '✅', '❌',
  '⚠️', '📌', '📝', '📎', '🔗', '🚀', '🐛', '⚡',
] as const
